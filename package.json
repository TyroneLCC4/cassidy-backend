{
  "name": "cassidy-backend",
  "version": "1.0.0",
  "description": "Backend services for Cassidy Prime Tech",
  "main": "api/send-otp.js", 
  "scripts": {
    "start": "echo 'Running serverless functions, no start command needed'",
    "test": "echo \"Error: no test specified\" && exit 1"
  },

  "dependencies": {
    "firebase": "^10.0.0",
    "twilio": "^5.0.0"
  },
  "engines": {
    "node": "20"
  }
}

